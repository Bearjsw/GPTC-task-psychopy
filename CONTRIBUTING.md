# 고칠 때

## 자극 문구를 바꾸려면

`stim/` 아래 CSV를 고친다. 코드는 건드리지 않는다.

고친 뒤에는 세 개를 돌려 본다.

```
python tools_check_design.py 300   설계 제약이 여전히 지켜지는지
python tools_preview.py 7          화면 문구가 넘치지 않는지
python tools_make_xlsx.py          엑셀 사본 갱신
```

## 브랜드명을 다시 뽑으려면

```
python tools_make_brands.py 150
```

`stim/brands.csv`가 갈린다. **뽑고 나면 사람이 한 번 훑어야 한다.** 어색하거나
뜻이 읽히거나 불쾌한 행을 지우면 된다. 72개 이상 남으면 실험은 돌아간다.

## 시행 수를 바꾸려면

`GPTC_task.py` 맨 위 `CFG`의 숫자를 고친다.

```
시행 수 = (제품군 수 - 제외 수) x 세트 수 x 반복 수
```

정보원 수로 나눠떨어져야 한다. 안 떨어지면 `tools_check_design.py`가 잡아 준다.

## 화면을 바꾸려면

`GPTC_task.py`의 `info_stims`, `decision_stims`, `header`를 본다. 좌표와 글자
크기는 전부 `CFG`에 있다. 바꾼 뒤에는 눈으로 확인한다.

```
python GPTC_task.py --windowed
python tools_capture_screens.py 폴더    화면을 한 장씩 PNG로
python GPTC_task.py --record 확인용     자동 진행을 4배속 영상으로
```

---

# 문제가 생기면

**PsychoPy가 import 단계에서 죽는다 (UnicodeDecodeError)**
한국어 윈도우에서 `psychopy.gui`가 부르는 i18next가 번역 JSON을 열 때 인코딩을
지정하지 않아 cp949로 읽다가 터진다. `GPTC_task.py`가 UTF-8 모드로 스스로 다시
띄워서 피해 간다. 손댈 것은 없다.

**글자가 하나도 안 그려진다**
PsychoPy가 윈도우에서 고정해 둔 pyglet 1.4.11이 Python 3.12에서 깨진다.
`TextStim` 대신 `TextBox2`를 써서 피했다. 스탠드얼론(Python 3.10)에서는 둘 다
되지만 코드는 그대로 쓰면 된다.

**엑셀에서 한글이 깨진다**
CSV에 BOM이 빠진 것이다. 이 저장소의 CSV는 전부 BOM을 붙여 두었고, 과제가 쓰는
데이터 CSV도 마찬가지다. 새로 만드는 CSV는 `utf-8-sig`로 저장한다.

---

# 설계 메모

왜 이렇게 정했는지. 바꾸기 전에 한 번 읽어 볼 것.

**브랜드명을 무작위 알파벳으로 만들지 않는다**
한때 알파벳 4~5자를 그 자리에서 뽑아 썼다. 연구 전체로 보면 2만 개가 만들어지는데
그중 42.6%는 모음이 아예 없고 62.8%는 자음이 세 개 넘게 붙었다. 금칙어 열넷으로
이만 개를 막는 구조라 사람이 한 번도 보지 않은 이름이 참가자에게 나갔다.
읽히지 않는 이름은 싸구려로 읽혀서 구매 의향을 바닥에 깔 수도 있다. 그래서
목록을 미리 확정하고 검수받는 쪽으로 바꿨다.

**제품군에서 FOOD와 HOBBY를 뺐다**
음식은 브랜드와 원산지가 판단의 전부라 가상 브랜드가 안 통한다. OTT 구독,
헬스장, 스팀 게임은 물건이 아니어서 가상 브랜드를 붙일 수 없고, 남는 퍼즐
하나로는 대분류당 카테고리 2개를 못 채운다.

**세트 안 세 후보의 매력도가 비슷해야 한다**
한쪽이 확실히 나으면 2·3등을 추천하는 것이 대놓고 나쁜 걸 권하는 일이 된다.
가장 중요한 행동 지표가 거기서 죽는다.

**가격은 브랜드가 아니라 세트 단위로 붙는다**
세 후보가 같은 가격을 쓰기 때문에 어느 후보가 추천되든 결정 국면의 가격이
같다. 추천과 가격이 엉키지 않는다.

**간섭 과제는 PsychoPy가 아니라 구글 설문에 있다**
두 국면 평정 블록 **사이**가 제자리다. 두 국면에서 묻는 문항이 글자 그대로
같아서, 붙여 놓으면 앞에 적은 숫자를 그대로 옮겨 적기 쉽다. 과제 끝에 두면
두 블록이 붙어서 진행되어 원래 목적을 못 한다.

**UT/HE는 제품이 아니라 문구 단위다**
Rowen의 옛 `product_list.csv`는 제품을 실용재·쾌락재로 갈랐다. 발표 자료 19쪽이
"Not the product type, but within the text"로 층위를 옮겼다. 지금은 제품군마다
실용 3개와 감성 3개를 두고 세트가 그중 셋을 뽑는다. 균형은 안 맞추고 기록만
한다(`DetailTypes`, `N_UT`, `N_HE`). 프라이밍을 주장에 쓸 거면 균형을 걸어야 한다.

**아직 안 정해진 것**
- 처리 동기 문항은 임시 문구다. 원 설계 문서에 이름만 있고 문구가 없었다
- 자극 48개는 사전 점검(norming)을 안 거쳤다
- 시행 길이는 정보 5초, 결정 4초로 잡아 9분이 나왔다. 실측 후 조정한다

**이전 버전**
`_이전버전_전체세션판/`에 세 세션을 전부 PsychoPy로 돌리던 판이 있다. 유사도,
국면별 평정, 조작 점검, 간섭 과제가 PsychoPy로 구현돼 있다. 설문을 다시 코드로
가져올 일이 생기면 거기서 꺼내 쓴다.
